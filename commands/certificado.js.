function gerarCertificado(nomeUsuario, trilha) {
    const dataAtual = new Date().toLocaleDateString('pt-BR');

    console.log("==================================================");
    console.log("             CERTIFICADO DE CONCLUSÃO              ");
    console.log("==================================================");
    console.log(` Certificamos que: ${nomeUsuario}`);
    console.log(` Concluiu com êxito a trilha de: ${trilha}`);
    console.log(` Emitido em: ${dataAtual}`);
    console.log(" Projeto: Geo-Explorer | Apoio: IBM Bob");
    console.log("==================================================");
}

const args = process.argv.slice(2);
const usuario = args[0] || 'Estudante';
const trilhaNome = args[1] || 'IBM Bob';

gerarCertificado(usuario, trilhaNome);
